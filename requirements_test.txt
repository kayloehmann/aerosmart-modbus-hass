-r requirements.txt
pytest-homeassistant-custom-component
pytest-asyncio
# Keep both transport packages explicit, just like manifest.json. Home Assistant
# checks whether the base distribution is installed without validating extras.
modbus-connection==3.9.0
tmodbus==0.5.0
mypy
