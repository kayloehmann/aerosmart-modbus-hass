-r requirements.txt
pytest-homeassistant-custom-component
pytest-asyncio
# The integration owns this backend directly; tests replace it with the
# package's in-memory mock.
modbus-connection[tmodbus]
mypy
